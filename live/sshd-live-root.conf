# Live installer: allow SSH login as root with the autogenerated password.
# The password is created per boot by live-ssh-setup.service and shown on the
# live user's console at login. The medium is ephemeral, so this never persists.
PermitRootLogin yes
PasswordAuthentication yes
